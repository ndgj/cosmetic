<% @post_images.each do |post_image| %>
  <div>
    <%= link_to post_image_path(@post_images.id) do %> 
    <%= image_tag @post_images.get_image %>
  <% end %>
    <p>投稿ユーザー画像：<%= image_tag @post_images.user.get_profile_image(100,100) %></p>
    <p>タイトル：<%= @post_images.title %></p>
    <p>キャプション：<%= @post_images.body %></p>
    <p><%= link_to "#{@post_images.post_comments.count} コメント", post_image_path(@post_images.id) %></p>
  </div>
<% end %>